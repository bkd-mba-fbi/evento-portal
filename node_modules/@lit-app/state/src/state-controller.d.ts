import { ReactiveController, ReactiveControllerHost } from 'lit';
import { State } from './state.js';
/**
 * A reactive-controller holding a state
 */
type callback = () => void | EventListener;
export declare class StateController<T extends State> implements ReactiveController {
    protected host: ReactiveControllerHost;
    private state;
    callback: callback;
    constructor(host: ReactiveControllerHost, state: T, cb?: callback);
    hostConnected(): void;
    hostDisconnected(): void;
}
export {};
//# sourceMappingURL=state-controller.d.ts.map