/**
 * @license
 * Copyright 2020 Google LLC
 * SPDX-License-Identifier: BSD-3-Clause
 */
import type { Config } from '../types/config.js';
import type { RuntimeOutputConfig } from '../types/modes.js';
import { LitLocalizer } from '../index.js';
/**
 * Localizes a Lit project in runtime mode.
 */
export declare class RuntimeLitLocalizer extends LitLocalizer {
    config: Config & {
        output: RuntimeOutputConfig;
    };
    constructor(config: Config & {
        output: RuntimeOutputConfig;
    });
    build(): Promise<void>;
}
